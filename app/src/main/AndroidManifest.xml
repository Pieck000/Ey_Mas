<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.isproject.ey">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ey">
        <activity android:name=".RecuperarActivity"></activity>
        <activity android:name=".EditeventoActivity" />
        <activity
            android:name=".UimainActivity"
            android:label="@string/title_activity_uimain"
            android:theme="@style/Theme.Ey.NoActionBar" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".PayActivity" />
        <activity android:name=".EventoActivity" />
        <activity android:name=".PaquetesActivity" />
        <activity android:name=".RegistroActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>